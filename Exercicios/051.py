primeiroTermo = int(input('Primeiro termo: '))
razão = int(input('Razão: '))
decimoTermo = primeiroTermo + 9 * razão
for i in range(primeiroTermo, decimoTermo + razão, razão):
    print('{} ' .format(c))
